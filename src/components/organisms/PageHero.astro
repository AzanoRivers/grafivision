---
import Badge from "@/components/atoms/Badge.astro";
import Img from "@/components/atoms/Img.astro";

interface Props {
  title: string;
  subtitle?: string;
  badge?: string;
  image?: string;
  class?: string;
}

const {
  title,
  subtitle,
  badge,
  image = "/images/banner/m-banner-1.png",
  class: className,
} = Astro.props;
---

<section
  class:list={[
    "relative flex items-center overflow-hidden bg-[#0e0002] py-20 xl:py-28",
    className,
  ]}
>
  <!-- Background image with warm-branded overlay + parallax -->
  <div class="absolute inset-0 overflow-hidden" aria-hidden="true">
    <Img
      src={image}
      alt=""
      width={1440}
      height={400}
      class="absolute w-full object-cover opacity-45 will-change-transform"
      style="top:-40%;height:180%"
      loading="eager"
      fetchpriority="high"
      data-parallax-img
      data-parallax-speed="0.45"
    />
    <!-- Layer 1: main legibility gradient — warm crimson-dark, fades right -->
    <div
      class="absolute inset-0 bg-linear-to-r from-[#0e0002]/92 via-[#0e0002]/58 to-[#0e0002]/12"
    >
    </div>
    <!-- Layer 2: brand-red bottom warmth — bridges to light sections below -->
    <div
      class="absolute inset-0 bg-linear-to-t from-brand-red/18 via-brand-red/4 to-transparent"
    >
    </div>
  </div>

  <div class="container-content section-padding relative z-10">
    {
      badge && (
        <Badge variant="red" size="md" class="mb-5">
          {badge}
        </Badge>
      )
    }
    <h1
      class="font-display text-4xl font-bold text-white xl:text-6xl max-w-2xl"
    >
      {title}
    </h1>
    {subtitle && <p class="mt-5 text-lg text-white/70 max-w-xl">{subtitle}</p>}
  </div>
</section>
